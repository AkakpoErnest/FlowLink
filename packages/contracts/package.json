{
  "name": "@flowlink/contracts",
  "version": "0.1.0",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy:sepolia": "forge script script/DeploySepolia.s.sol --rpc-url $SEPOLIA_RPC_URL --private-key $PRIVATE_KEY --broadcast --verify",
    "deploy:amoy": "forge script script/DeployAmoy.s.sol --rpc-url $AMOY_RPC_URL --private-key $PRIVATE_KEY --broadcast --verify",
    "typechain": "typechain --target ethers-v6 --out-dir types 'out/**/*.json'"
  },
  "devDependencies": {
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "typechain": "^8.3.1",
    "viem": "^1.19.9"
  }
}

